package org.goldenport.cli.model

import org.goldenport.Consequence
import org.goldenport.protocol.*

/*
 * @since   Dec. 22, 2025
 * @version Dec. 24, 2025
 * @author  ASAMI, Tomoharu
 */
// case class Service(name: String, operations: Seq[Operation])

// case class Operation(name: String, endpoint: String, schema: OperationSchema) {
//   def parseOption(args: List[String]): Option[Consequence[OperationRequest]] = {
//     args match {
//       case x :: xs if x == name => ??? // schema.parse(xs)
//       case _ => None
//     }
//   }
// }

// case class OperationSchema(input: Schema, output: Schema) {
//   def parse(args: List[String]): Consequence[OperationRequest] = {

//   }
// }

// case class ServiceRegistry(services: Seq[Service], defaultService: Option[String])
